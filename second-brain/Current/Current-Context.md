---
type: current-context
status: active
tags: [priority/high, area/infra, area/frontend]
created: 2026-07-20
updated: 2026-08-04
related: ["[[Deployment-Notes]]", "[[Known-Issues]]", "[[Lessons-Learned]]", "[[UI-UX-Guidelines]]", "[[Workspace-UI-Design-Decisions]]", "[[FEAT-preview-tabs]]", "[[FEAT-citation-sources]]", "[[FEAT-sign-in]]", "[[BUG-0001-narrow-query-refusal-single-chunk]]", "[[BUG-0002-inline-citation-leak-in-answer-text]]"]
---

# Current Context

## Active priority
Auth UX: unsigned users must land on `/sign-in` (client `RequireAuth` gate shipped). Still open: false "not enough grounded information" refusals on production — usually sparse/scanned extracts (OCR deferred), not a dead retriever. Files UI now shows status + scanned warning; near-empty scanned uploads fail ingest. User should re-upload text-native files for the store that was refusing.

## What's true right now (2026-08-04)
- **Large pass across chat, files, and previewer — committed** (`36bc0f1`, `f280c47` on `tolu-implementations`; not pushed). Not yet run through Alembic anywhere but the local dev DB (stamped to head directly) — Railway's `preDeployCommand` runs `alembic upgrade head` automatically on next deploy, so production picks up both new migrations (`chat_settings`, `files.preview_markdown`) without a manual step. Grouped by area:
  - **Chat**: framer-motion typing indicator (`components/ask/typing-indicator.tsx`) while a reply is in flight; markdown-rendered assistant answers (`components/ask/markdown-message.tsx`, `react-markdown`); shadcn `Spinner` added (`components/ui/spinner.tsx`). Chat search (filters current session's messages) and a History popover (switch between open sessions) are now wired — previously dead buttons.
  - **Configure Chat is now real, not local-only, and per-user**: `chat_settings` table keyed on `user_id` (business_id kept as a required, indexed scoping column per CLAUDE.md §2.4) + `GET/PUT /chat-settings`, derived from the signed-in user's JWT — each person gets their own persona, not one shared per business (reversed from an earlier same-session "global" call once the actual product intent came up — each staff member may want a different style). `sage_agent.py` layers the saved goal/response-length/custom-instructions onto `SYSTEM_PROMPT` via a dynamic `@agent.system_prompt` function (never replaces the grounding/citation rules). See `app/chat_settings/`.
  - **PDF ingestion was hard-failing on every upload** (`InvalidCxxCompiler: No working C++ compiler found`) — docling's layout model defaults to `torch.compile()`, which needs a real C++ toolchain this environment doesn't have. Fixed by disabling `docling_settings.inference.compile_torch_models` in `_build_converter()`. See [[Lessons-Learned#2026-08-04 — Docling's layout model defaults to torch.compile(), which hard-fails PDF ingestion without a C++ compiler]].
  - **Files panel**: Sort (name asc/desc), Bookmarks filter, file search, and Auto-reveal-current-file (scrolls+highlights the active preview tab's row) are now wired. "New folder", "Auto-Sort", "Collapse all" are still no-ops — they need an actual folder/tree data model that doesn't exist yet, not just an onClick handler; flagged, not built.
  - **File row**: kebab (⋮) button now appears on hover/focus next to the bookmark icon, opening the same menu as right-click (previously right-click-only, undiscoverable).
  - **Upload validation**: a `.txt`/`.md` upload whose content magic-sniffs as some other *text* flavor (e.g. `text/html` because it holds HTML source) is now accepted — narrow carve-out in `app/files/service.py`, still rejects real binaries disguised with a safe extension. All upload/replace/delete errors are now toasts (matching the existing "OCR is off" pattern) instead of a lingering inline banner in the files panel.
  - **Previewer (PDF)**: fixed a real bug where two duplicate tabs of the same file shared one mounted viewer instance (no `key` on `FilePreviewRouter`) — scrolling one silently scrolled the other; now keyed by `tabId`. Fixed continuous-scroll-mode visual twitching by rAF-batching the scroll→viewState write (was firing a React re-render of the whole stacked-pages tree on every native scroll event). Added a "Fit to page" zoom button. Citation-click highlighting is wired for text/md/docx (`<mark>` + scroll-into-view via `viewState.highlight` char offsets) — **still not implemented for PDF** (needs pdf.js text-layer↔chunk mapping, deferred).
  - **Preview tab strip**: reverses the "no horizontal scrollbar, continuous flex-shrink only" call in [[FEAT-preview-tabs]]/[[Workspace-UI-Design-Decisions]] — with enough open tabs, compression alone let later tabs clip off-screen with no way to reach them. Tab lane is now `overflow-x-auto` with a thin scrollbar once tabs hit their intrinsic floor; compression behavior for the common (few-tabs) case is unchanged. **Design docs not yet updated to match** — still say "no horizontal scrollbar."
  - **Docx preview redesign**: fixed a real Docling bug (`table.export_to_dataframe()` without `doc=` silently returns `"<!-- rich cell -->"` placeholder text for any rich-formatted cell, and `doc.export_to_text()` already includes tables natively so the separate sentence-linearization pass was duplicating every table) — see [[Lessons-Learned#2026-08-04 — Docling's TableItem.export_to_dataframe() silently returns "<!-- rich cell -->" placeholders without a doc= argument]]. Beyond the bug fix, docx preview now renders as real HTML tables (`remark-gfm` + `react-markdown`) instead of a flat `<pre>` text dump: new `files.preview_markdown` column stores Docling's own `export_to_markdown()` export (human-preview-only, separate from the RAG-chunked text, which is unchanged) via new `GET /files/{id}/preview`. Old already-ingested docx files fall back to the flat-text viewer until replaced/re-uploaded (`preview_markdown` is null for them). **Note:** citation highlighting does NOT work in markdown-preview mode for docx (char offsets are into the RAG text, not the markdown export) — only the flat-text fallback path supports it.
  - **Settings dialog**: fixed a real spacing bug in confirm-style dialogs (Delete file, etc.) — `ShellDialog`'s 20rem forced min-height was being applied even to header+footer-only dialogs with no body, stranding the footer far below the content with no filler between. Now gated to `kind="form"` only. Also removed the native scrollbar "rail" (opaque track) from the Settings dialog's Account/General tabs via a new `scrollbar-thin` utility (globals.css).
  - **Copy pass**: several retail-store-specific strings in frontend microcopy and two backend-surfaced chat strings (`REFUSAL_MESSAGE`, `LIMIT_MESSAGE`) were generalized to not assume "store floor" specifically. **Deliberately left untouched**: the backend `SYSTEM_PROMPT` persona ("Sage, a question-answering assistant for a boutique retail store's staff") and `Product-Vision.md`'s "boutique retail" MVP framing — that's a locked beachhead decision ([[0007-boutique-retail-mvp-beachhead]]), not incidental copy, and changing it wasn't asked for.
- Two new Alembic migrations this session: `chat_settings` table, `files.preview_markdown` column. **Neither has been run via `alembic upgrade head` anywhere** — the local dev DB was patched directly (`ALTER TABLE`/`create_all`, alembic version table was never actually initialized against it, matching the pre-existing pattern in `db.py`'s docstring). Production still needs a real `alembic upgrade head` before these features work there.
- All 62 backend tests and all frontend checks (`tsc`, `eslint`, `vitest` 77/77, `next build`) pass as of this session's last verification. Nothing has been committed yet — still working-tree only, spanning `backend/app/{agent,chat_settings,files,ingestion}/`, `backend/alembic/versions/`, and most of `frontend/src/{app,components,hooks,lib}/`.

## What's true right now (2026-08-03)
- **`main` merged into `tolu-implementations`** (`9a72821`): the two branches had diverged since `cea3038` (2026-07-19) — `main` had gained the entire Phase 9 file-viewer build (PDF via `react-pdf`, text/image/docx, `FilePreviewRouter`) plus a batch of UI-primitive refactors (`ShellDialog`, `Popover`, `TagInput`, `EmptyState`, `Input` with `trailing`) that `tolu-implementations` never received, while `tolu-implementations` had this session's chat-tabs/organization/Dockerfile work that `main` never received. Real conflicts were limited to import lines in `page.tsx`/`accounts-table.tsx` (bodies auto-merged cleanly) plus frontmatter/entry-ordering in two vault docs. Verified post-merge: `tsc`/`eslint`/`vitest` (77/77)/`next build` clean, plus a live browser pass confirming the previewer now renders real content (not "Preview coming soon"), chat tabs and the organization page still work. Backend test suite (`pytest`) was not verified locally — the local run hung indefinitely with zero output for an unclear reason unrelated to the merge (no conflicts touched backend files); worth a follow-up check before deploying backend changes.
- **Auth gate:** `/` wrapped in `RequireAuth` — no `sage_access_token` → `replace("/sign-in")`; children mount only after check. `/sign-in` sends already-signed-in users to `/`. Token still `sessionStorage` (no middleware). See [[FEAT-sign-in]].
- Backend deploy live at commit `ca02e92` (memory mitigations + FlashRank cache_dir fix). Healthcheck passing; uvicorn on `:8080`.
- Live CORS: OPTIONS from `https://sage-frontend-production.up.railway.app` → **200** + `access-control-allow-origin`.
- PgBouncer `statement_cache_size=0` is on this image (ancestor commit `26a10a4`).
- Memory mitigations shipped and verified: `.txt`/`.md` bypass Docling, one shared `DocumentConverter`, `app.ml_gate.heavy_ml` semaphore serializes extract↔rerank, `CANDIDATE_POOL_SIZE=14`, native thread caps, FlashRank's baked model cache_dir fixed (was silently discarded, redownloading on every cold start).
- FlashRank reranker is **`ms-marco-MiniLM-L-12-v2`**, not TinyBERT — TinyBERT was tried for lower RAM but its score scale broke `trust.py`'s 0.35 threshold (real match scored 0.03), causing false refusals. Reverted same day; caught by `test_relevant_query_proceeds` + the eval suite before it shipped. See [[Lessons-Learned]].
- **Railway memory limit upgraded 1GB → 8GB** (user action) after code mitigations alone still left peak RSS at 762-927MB against the 1GB cap and the container OOM-crashed under just two sequential `/ask` calls. Post-upgrade: 5 sequential + 3 concurrent `/ask` requests all succeeded, peak RSS steady ~1.17GB (15% of new limit), zero crashes. See [[Known-Issues]], [[Lessons-Learned]].
- Frontend deployed as its own Railway service: `https://sage-frontend-production.up.railway.app` (manual `railway up`, not GitHub-auto-deployed — see next section).
- Production `/ask` refusals (`85280a06…`): retrieve `hits=8`, trust `top_score` ≤ 0.31; correlated with scanned PDF extract (`chars/page=136`, `chunks=1`). Threshold 0.35 kept — MiniLM scores real matches ≫ 0.35.
- Merged `origin/tolu-implementations` into `main` (2026-07-27): kept compact toasts + FormDialog settings shell; added ThemeProvider / Settings Theme tab.
- Toasts now portal to `document.body` at `z-[100]` (were invisible under dialogs / clipped in Ask column). See [[UI-UX-Guidelines#Toasts (application-owned)]].
- Toast host right inset fixed: `right-4` → `right-2` in `toast-provider.tsx`, to match the `px-2` inset shared by the header icon row and content grid — toast right edge was visibly misaligned with them. See [[Lessons-Learned]].
- Preview-stage tab design direction clarified for MVP: client-owned workspace tab store; clicking an already-open file focuses the existing tab; duplication is explicit only; duplicate tab instances may share a resource but own independent `viewState` (zoom/page/scroll); pinned tabs stay left and are protected (must unpin before close, survive `close all`); removed files become removed/error tabs instead of auto-closing; overflow mode is a tab-strip/workspace preference (`scroll` vs `pagination`) persisted via `localStorage`; pinned tabs may compress only down to icon + truncated filename + pin/unpin affordance + kebab, with full filename via tooltip. Durable note: [[Workspace-UI-Design-Decisions]].
- Preview-tabs pure state layer shipped: `frontend/src/lib/preview-tabs/` (types, reducer, selectors, storage, zustand store — first zustand usage and first test runner (vitest) in the repo), 65 passing tests, clean `tsc`/`eslint`. See [[FEAT-preview-tabs]].
- Preview-tabs **Phase 7 (tab-strip UI) shipped**: `frontend/src/components/preview-tabs/` (strip, pinned/unpinned lanes with compression + pagination/scroll, tab kebab, strip-settings kebab, stage placeholder/removed states, center panel), wired into `page.tsx`'s center panel and `FileList` row click → `openTab`. Reducer untouched (no bugs found). Verified: `npm run test` (65/65), `tsc --noEmit` clean, `npm run build` succeeds, and the new `preview-tabs/` files are individually eslint-clean. Could not do a live browser pass — no browser automation tool (chromium-cli/playwright) available in this environment; `npx playwright` stalled resolving packages and was abandoned. Two known caveats: an `overflowMode` SSR-hydration edge case, and pre-existing repo-wide `npm run lint` failures (10 errors, unrelated files, predate this work — see [[FEAT-preview-tabs#Known caveats from Phase 7]]).
- Preview-tabs tab-strip visual polish (uncommitted): continuous flex-shrink overflow (icon-only last resort via measured width); between-tab dividers only (hide L/R on hover/active); pin/close share one ghost button; no scroll/pagination strip settings. See [[Workspace-UI-Design-Decisions]].
- Preview-tabs **Phase 8 (lifecycle sync) shipped**: new pure selector `getResourceKeysToMarkRemoved(tabs, presentResourceKeys)` in `selectors.ts` (+4 tests, 69 total now) and a new hook `frontend/src/hooks/use-sync-removed-preview-tabs.ts` wired into `page.tsx` right after `useFileLibrary()`. Diffs `files` against open tabs on every `files`/`tabs` change and calls `markResourceRemoved` for anything missing — covers both explicit delete (synchronous) and the 2s poll (external disappearance). Title/fileType refresh on replace was already handled by `OPEN_TAB`, so nothing further was needed there. Verified: `tsc --noEmit`, `eslint` (new files clean; page.tsx's one pre-existing `getUserRole` lint error is unrelated, see caveats above), `npm run test` (69/69), `npm run build`. See [[FEAT-preview-tabs#Phase 8 — Lifecycle sync (implemented)]].
- Preview-tabs **Phase 9 (file viewers) shipped**: `react-pdf` PDF viewer (page/zoom/scroll), image zoom, txt/md plain text, docx via `GET /files/{id}/text`; debounced `updateViewState` (200ms); content cache by `resourceKey`. Stage placeholder replaced. Verified: `npm run test` (73/73), `tsc`, eslint on new files, `npm run build`. See [[FEAT-preview-tabs#Phase 9 — File viewers (implemented)]].
- **Citation sources infra shipped** ([[FEAT-citation-sources]]): `/ask` returns structured citations with `filename` + char offsets; chat shows Gurubase-style Sources badges (file-type icon + filename); click opens/focuses the preview tab and seeds `viewState.highlight` for Phase 9 viewers.
- Citation source badge (`frontend/src/components/ask/citation-sources.tsx`) was missing `cursor-pointer` — Tailwind v4 preflight resets `button { cursor: default }`. Fixed with `cursor-pointer`. See [[FEAT-citation-sources]].
- **Fixed** the "narrow query refused, broad query answered" false-refusal pattern (2026-07-30, [[BUG-0001-narrow-query-refusal-single-chunk]]): `chunk_text()` now splits short multi-paragraph docs along paragraph boundaries. Re-verified against the LOJJ.io/TurnUp repro. See bug note + eval cases.
- **Fixed** a raw citation-id leak into user-visible answer text ([[BUG-0002-inline-citation-leak-in-answer-text]]): tightened prompt + defensive `_strip_inline_citations()` in `answer.py`.
- **Neither BUG-0001 nor BUG-0002 is deployed to production yet.**

## Still open
1. User: re-upload **text-based** policy docs (`.txt` / `.md` / text PDF / `.docx`) for any store still refusing; delete scanned "Ready" files that can't be answered.
2. Deploy frontend file-list status/`looks_scanned` hint + backend near-empty-scanned fail (working tree).
3. User: set `sage-frontend` Root Directory = `frontend` in the Railway dashboard, then reconnect its GitHub source (`railway service source connect --repo LOJJ-IO/Sage_v1 --branch tolu-implementations --service sage-frontend`) so future backend commits stop triggering doomed repo-root build attempts for it, and future frontend commits actually auto-deploy.
4. Optional cleanup: `front.html` and `.railway-config-pull-*` landed in `f01c190` — remove in a follow-up commit if undesired.
5. Dockerfile still reinstalls all dependencies (including CPU torch) from scratch on every deploy — `COPY app ./app` happens before `RUN pip install .`, invalidating Docker's build cache on every code change. Not urgent now that images push quickly, but worth reordering (stub-package install trick) if build times become annoying.
6. Local dev DB (`backend/.devdb`, port 55432) needs to be started manually (`pgsql/bin/pg_ctl.exe -D ./data -l ./pg.log -o "-p 55432" start`) before running the backend test suite locally — it doesn't survive a machine restart.
7. Preview-tabs: Phases 1–9 done (committing in slices). Next up — **Phase 10**: CI gate (`lint`/`test`/`tsc`/`build`) — blocked on a repo-wide `react-hooks` lint sweep first (see [[FEAT-preview-tabs#Known caveats from Phase 7]]). A live browser QA pass of the Phase 7–9 UI is still owed. Citation highlight consumption in viewers is still partial.
8. Deploy BUG-0001 / BUG-0002 backend fixes to production.
