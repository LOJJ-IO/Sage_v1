---
type: daily
status: active
tags: []
created: 2026-07-19
updated: 2026-07-27
related: ["[[FEAT-configure-settings]]", "[[FEAT-organization]]", "[[UI-UX-Guidelines]]", "[[Retrieval-Contracts]]", "[[Reusable-Patterns]]"]
---

# 2026-07-19 Sunday

## Focus
Compact iOS-style toast redesign; Settings Theme tab + dark mode; lock retrieval contracts for backend work

## Log
- Compacted `ToastCard`: `py-2`, icon+title/description row, 2px gap, no muted icon disc
- Dismiss → hover-only `X` overlapping top-left; removed toast action buttons
- Updated [[UI-UX-Guidelines#Toasts (application-owned)]]
- **Railway `compassionate-serenity`:** linked CLI; fixed service `Sage_v1` rootDirectory `frontend` → `backend`; set uvicorn start command + `/health` check; deployed backend via `railway up ./backend --path-as-root`. Still need Supabase `DATABASE_URL` (not localhost) + public domain. See [[Deployment-Notes]].
- **Browser verify Ask path:** Playwright Chromium on `localhost:3000` uploaded `lavender-return-policy.txt`, waited for `indexed`, asked return-window question — UI showed correct 45-day answer + hash citation. API also reproduced Known-Issues dress-code refusal on `store-policy.txt` (not a new bug).
- Branch `tolu-implementations` created from `main`
- Fixed dark mode CSS: `:root.dark` after `:root` so dark tokens actually win; root `ThemeProvider` for app-wide + System preference
- Settings dialog: Theme tab alongside General / Account (Light / Dark / System pills)
- Dark tokens: muted = well, background = elevated panels; stronger muted-foreground; `color-scheme: dark`
- Sign-in + organization layouts: `bg-neutral-100` → `bg-muted`
- Cleared Organization demo seed (`DEMO_ACCOUNTS = []`); empty-state on `/organization`
- Added always-apply Cursor rule `.cursor/rules/second-brain.mdc` (read vault before acting, write back after)
- **Chat send oversight:** Ask panel send button had no `onClick` (message state went nowhere). Wired send + Enter → local message thread + stub assistant reply; New chat clears. Still not calling FastAPI `/ask`.

## Decisions made
- Toast CTAs live in-page/dialogs, not on the toast card
- Theme preference local-only for now (no `/me/preferences` yet)
- Keep Configure-chat pill pattern for theme options in Settings
- Backend on this branch is **contract-first**: adjust imports/helpers, never assertions; red until retriever exists is expected
- Second-brain usage is mandatory for agents (not optional context)
- Chat UI can show a local thread before backend wiring; stub reply must say so plainly (no fake grounded answers)

## Blockers / open questions
-

## Promote to permanent notes
- Toast dismiss/layout rules → [[UI-UX-Guidelines]]
- [[FEAT-configure-settings]], [[FEAT-organization]], [[UI-UX-Guidelines]], [[Current-Context]], [[Retrieval-Contracts]]
- Cursor rule: `.cursor/rules/second-brain.mdc`
