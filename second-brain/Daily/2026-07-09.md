---
type: daily
status: active
tags: [area/frontend]
created: 2026-07-09
updated: 2026-07-09
related: ["[[Current-Context]]", "[[UI-UX-Guidelines]]", "[[FEAT-organization]]"]
---

# 2026-07-09 Thursday

## Focus
Light-only UI cleanup + organization visual fixes + primary button texture

## Log
- Removed theme system: boot script, `lib/theme.ts`, `hooks/use-theme.ts`, Appearance submenu, `.dark` CSS + `dark:` utilities
- Organization page: `codicon-chevron-left` (was invalid `-compact`); role chips active=admin style / inactive=muted+strikethrough; Badge merges via `twMerge`
- Primary `Button` (`default`): pill radius, soft gradient, diffuse shadow, inset highlight
- Shell icon clusters: `HeaderIconGroup` pill on top-bar + panel toolbars; right header: admin-only Organization (`codicon-organization`) + Profile in one pill, Collapse in a separate pill (`gap-2`); Ask panel New chat is icon-only in the group; both panel headers `h-14`
- Ask panel header: editable chat title flush-left (`left-2`); tools pill right-aligned (`pr-2`); title truncates before the pill
- Profile menu: Organization entry removed (header button owns it)
- Shell chrome: floating rounded panels (`rounded-2xl bg-background`) on `bg-muted` well; 8px invisible resize gutters; middle panel same surface; disclaimer footer under panels; white top header removed (toolbar on well)
- AI panel zero-state: no-docs copy + upload CTA; input disabled until files exist; example-driven placeholder
- Renamed Manage team → **Organization** (US spelling); route `/manage-team` → `/organization`; `OrganizationView` + `FEAT-organization`

## Decisions made
- Theme switching deferred — light-only until product revisits themes
- Icon toolbars use pill shells; space separate pills with `gap-2` instead of `|` dividers
- Shell chrome: NotebookLM-inspired floating panels (chrome only); supersedes flush VS Code seams / visible 2px divider for this look

## Blockers / open questions
-

## Promote to permanent notes
- [[Current-Context]] + [[UI-UX-Guidelines]] updated
