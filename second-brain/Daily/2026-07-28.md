---
type: daily
status: active
tags: [area/frontend, area/design]
created: 2026-07-28
updated: 2026-07-28
related: ["[[Current-Context]]", "[[Workspace-UI-Design-Decisions]]", "[[UI-UX-Guidelines]]"]
---

# 2026-07-28 Tuesday

## Focus
Clarify the middle-pane file preview tab model before implementation so tab behavior is state-driven instead of ad hoc.

## Log
- Captured preview-tab decisions from design session: explicit duplication only; clicking an already-open file focuses the most recently active matching tab; duplicate instances may share a resource while owning independent view state.
- Locked pinning semantics: pinned tabs stay left, are protected from direct close, survive `close all`, and require explicit unpin before close.
- Locked removed-file behavior: deleted/unavailable files become removed/error tabs instead of auto-closing; removed tabs cannot be duplicated.
- Locked overflow ownership: `pagination` vs `free horizontal scroll` is a tab-strip/workspace preference, persisted via browser storage for MVP; open tabs themselves do not persist across refresh in MVP.

## Decisions made
- Updated [[Workspace-UI-Design-Decisions]] to replace the earlier "3 visible tabs" rule with the new overflow/pinning/view-state model.
- Updated [[UI-UX-Guidelines]] with the ownership split between tab-strip-level and tab-level controls.

## Blockers / open questions
- Need an implementation-ready reducer/state-machine spec for tab actions (`open`, `focus`, `duplicate`, `close`, `pin`, `unpin`, `mark removed`, `set overflow mode`) before coding.
- Need to choose exact minimum-width measurements and responsive behavior once visual implementation starts.

## Promote to permanent notes
- Preview-tab interaction rules promoted to [[Workspace-UI-Design-Decisions]] and [[UI-UX-Guidelines]].
