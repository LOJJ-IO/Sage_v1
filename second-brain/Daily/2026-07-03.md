---
type: daily
status: active
tags: [area/frontend]
created: 2026-07-03
updated: 2026-07-03
related: ["[[FEAT-app-shell-layout]]", "[[UI-UX-Guidelines]]", "[[Current-Context]]", "[[Lessons-Learned]]"]
---

# 2026-07-03 Friday

## Focus
Dark mode, panel empty states, right-panel chat input, and header polish for the app shell.

## Log
- **Dark mode** — `isDark` state toggles `dark` class on `document.documentElement`; sun/moon toggle in global header right group (`837e8335` builds on this).
- **Empty states** — shadcn `Empty` for left panel (`FilesEmptyState`) and right panel (`AskAiEmptyState`); CVA + `components.json` added.
- **Right panel chat input** — `AskAiChatInput` with rounded pill input, voice + send buttons (UI only, no backend).
- **Global styles** — design tokens in `globals.css` for light/dark; header and panels use `dark:` variants.
- **Header vertical separator (right group)** — commit `837e8335`: added `mx-2 h-5 w-px bg-neutral-200 dark:bg-neutral-700` divider between dark-mode toggle and right sidebar collapse, mirroring the left-group separator after the left sidebar toggle.
- **Scrollbar fix** — collapsing a side panel no longer causes global viewport scrollbars; shell locked to viewport + divider tracks/handles hidden when panel collapsed. See [[Lessons-Learned#2026-07-03 — Global scrollbars when collapsing side panels]] and [[Reusable-Patterns#Viewport-locked app shell]].
- **`MIN_SIDE_WIDTH` 14%** — raised from 12% so left-panel tooltips (e.g. Auto-reveal) aren't clipped at minimum resize. See [[Lessons-Learned#2026-07-03 — Panel tooltips clipped at minimum resize width]].

## Decisions made
- Header vertical separators use the same token on both sides: light `neutral-200`, dark `neutral-700`.
- Dark mode is client-side class toggle (not system preference yet) — sun/moon in header right group, before the separator + collapse toggle.

## Blockers / open questions
- Empty-state upload button and chat input are UI-only — no backend wiring.
- Dark mode does not yet respect `prefers-color-scheme` or persist to localStorage.

## Promote to permanent notes
- [[Current-Context]] — refreshed active state
- [[UI-UX-Guidelines]] — header separator pattern (both sides) + dark mode notes
- [[FEAT-app-shell-layout]] — right header group, empty states, chat input
- [[Lessons-Learned#2026-07-03 — Global scrollbars when collapsing side panels]] — viewport lock + hide dividers when collapsed
- [[Reusable-Patterns]] — viewport-locked shell, header separator, updated icon constants
