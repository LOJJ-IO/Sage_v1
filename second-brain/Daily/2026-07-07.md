---
type: daily
status: active
tags: [area/frontend]
created: 2026-07-07
updated: 2026-07-07
related: ["[[Stacking-Contexts-and-Portals]]", "[[Lessons-Learned]]", "[[Reusable-Patterns]]", "[[Current-Context]]"]
---

# 2026-07-07 Tuesday

## Focus
Document tooltip rendering / stacking-context learnings and React Hooks fundamentals in second-brain.

## Log
- **Header tooltip stacking context** — `z-50` on tooltip doesn't escape header layer; grid panel paints over it. **Fixed:** portaled all `HeaderIconButton` tooltips via shadcn/Base UI `Tooltip`. See [[Stacking-Contexts-and-Portals#Sage implementation]].
- **React Hooks** — `useState` is the Hook; functional updater `(current) => !current` preferred for toggles. Documented alongside stacking-context note.
- **Paint order foundation** — added to [[Stacking-Contexts-and-Portals]]: one-canvas paint order, DOM order, apartment/PNG/photo models, 3-question debug checklist.
- **Right panel header hierarchy** — New chat as labeled `Button size="sm"` (primary CTA); Search chats + History as `HeaderIconButton`; vertical divider between primary and utilities. Left panel stays all icon peers. See [[FEAT-app-shell-layout#Right panel internal header (`h-12` / 48px, centered controls)]].
- **Portaled tooltips shipped** — `frontend/src/components/ui/tooltip.tsx` (shadcn/Base UI + `variant="compact"`), `TooltipProvider` in `layout.tsx`, `HeaderIconButton` refactored in `page.tsx`.

## Decisions made
- Stacking-context vs overflow-clipping are separate tooltip bugs — keep both lessons linked, not merged.
- **Portal over `z-20` on header** — chosen fix for shell tooltips; one pattern fixes both stacking and overflow clipping.

## Blockers / open questions
- None for header tooltip stacking (resolved via portal).

## Promote to permanent notes
- [[Stacking-Contexts-and-Portals]] — full mental model (paint order, stacking contexts, paper sheets / PNG photos, portals, Hooks)
- [[Lessons-Learned#2026-07-07 — Header tooltips hidden behind grid panels (stacking context)]]
- [[Lessons-Learned#2026-07-07 — React Hooks: `useState` and functional updaters]]
- [[Reusable-Patterns#HeaderIconButton (icon + compact tooltip)]] — stacking / visibility guidance
