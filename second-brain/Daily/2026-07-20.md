---
type: daily
status: active
tags: [daily]
created: 2026-07-20
updated: 2026-07-20
related: ["[[Current-Context]]"]
---

# 2026-07-20

- Fixed Railway deploy healthcheck: `startCommand` needed `/bin/sh -c` so `$PORT` expands (was literal → uvicorn crash). See [[Lessons-Learned]].
- Diagnosed 32m FAILED deploys: Docker build OK, dies on `image push` (torch/docling image). Live CORS still rejects Railway frontend origin despite var set — need restart. Frontend *does* call backend (`NEXT_PUBLIC_API_URL` in bundle). See [[Deployment-Notes]], [[Current-Context]].
- Shipped CPU-torch multi-stage Dockerfile; deploy `b8c66a4d` SUCCESS. CORS from Railway frontend origin now 200. PgBouncer fix live. User still setting `sage-frontend` Root Directory = `frontend`.
- Implemented backend RAM mitigations (not yet committed/redeployed): plain-text Docling bypass + converter singleton; `app/ml_gate.py` serializes extract↔rerank; pool 14; Dockerfile thread caps. See [[Known-Issues]], [[Deployment-Notes]].
- FlashRank TinyBERT swap (part of the RAM pass above) broke the trust threshold — caught by the test suite, reverted to MiniLM-L-12 same day before committing. See [[Lessons-Learned]].
- Ran full local test suite against the combined change set (Cursor agent's RAM mitigations + this revert): 27/27 passing. Committing and triggering a Railway redeploy next.
- Diagnosed production "not enough grounded information" refusals: retrieve returns hits, trust top_score under 0.35; correlated with scanned PDF (OCR deferred, 1 chunk). Files UI now shows status/`looks_scanned`; near-empty scanned extracts fail ingest. Do not lower threshold without evals.
